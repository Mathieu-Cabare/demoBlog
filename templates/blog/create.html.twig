{% extends "base.html.twig" %}

{# on hérite du template 'base.html.twig' #}
{# tout le contenu du bloc body sera envoyé dans le template 'base.html.twig' #}

{% form_theme formArticle 'bootstrap_4_layout.html.twig' %} {# fait le lien avec le fichier twig.yaml pour appliquer les theme bootstrap#}

{% block body %}

<h1 class="display-4 text-center my-4">Création d'un article</h1>

{# <form method="post" class="col-md-8 mx-auto">
    <div class="form-group">
        <div class="mx-auto mt-3">
        <label class="" for="title">Ajouter un Titre</label>
        <input type="text" class="form-control" name="title" id="title">
        </div>
    </div>
    <div class="form-group">
        <div class=" mx-auto mt-3">
        <label class="" for="image">Ajouter une Image</label>
        <input type="text" class="form-control" name="image" id="image">
        </div>
    </div>
    <div class="form-group">
        <div class=" mx-auto mt-3">
            <label class="" for="content">Ajouter votre article</label>
            <textarea class="form-control"  name="content" id="content" rows="14" ></textarea>
        </div>
    </div>
    <button type="submit" class="btn btn-secondary">Enregistrer</button>
</form> #}

{# {{ form(formArticle) }} permet d'affichier toutle formulaire #}


{{ form_start(formArticle, { 'attr' : {'class' : "col-md-6 mx-auto"} }) }}
    {# <div class="row">
        <div class="col-md-8 mx-auto"> 
            {{ form_label(formArticle.title, 'Titre' ) }}
            {{ form_widget(formArticle.title) }}
        </div>
    </div> 

     <div class="row">
        <div class="col-md-8 mx-auto"> 
            {{ form_label(formArticle.title, 'Contenu') }}
            {{ form_widget(formArticle.content) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-8 mx-auto">
            {{ form_label(formArticle.title, 'URL de l\'image') }}
            {{ form_widget(formArticle.image) }}
        </div>
    </div> #}

    {{form_row(formArticle.title, { 'attr' : {'placeholder': "Titre de l'article"} }) }}

    {{form_row(formArticle.category) }}

    {{form_row(formArticle.content, { 'attr' : {'placeholder': "Contenu de l'article", "rows":12} }) }}

    {{form_row(formArticle.image, { 'attr' : {'placeholder': "Chemin de la photo"} }) }}


    <button type="submit" class='btn btn-secondary'>
    {% if editMode %}
        Enregistrer les modifications   
    {% else  %}
        Ajouter un article 
    {% endif %}
    </button>


{{ form_end(formArticle) }}


    
<form>
  
    {# cols="50" {
    'label': 'Your name',
    'label_attr': {'class': 'foo'}#}
</form>

{% endblock %}